#include <iostream>
#include <cassert>
using std::cerr;
using std::cout;
using std::endl;
#include <math.h>
#include "../Controller.h"

using namespace std;
int main()
{
    // unit testing with run time asserts
    // controller tests:

    // TEST-0 : test the quarternion rotation matrix

    // assert(TODO);

    return 0;
}
